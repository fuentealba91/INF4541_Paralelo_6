import { FiltradoEstadoPipe } from './filtrado-estado.pipe';

describe('FiltradoEstadoPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltradoEstadoPipe();
    expect(pipe).toBeTruthy();
  });
});
